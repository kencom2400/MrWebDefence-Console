## CI修正: common.shのMySQL接続関数に--protocol=TCPオプションを追加

「Wait for MySQL to be ready」ステップは成功していますが、「Run database migrations」ステップでMySQLへの接続に失敗していた問題を修正しました。

### 🔧 問題の原因

`common.sh`のMySQL接続関数（`check_mysql_connection`、`create_database`、`check_utf8mb4_config`）で`--protocol=TCP`オプションが使用されていなかったため、GitHub Actionsのサービスコンテナへの接続に失敗していました。

### ✅ 修正内容

1. **`check_mysql_connection`関数の修正**
   - `--protocol=TCP`オプションを追加
   - エラーメッセージを表示するように修正

2. **`create_database`関数の修正**
   - `--protocol=TCP`オプションを追加

3. **`check_utf8mb4_config`関数の修正**
   - `--protocol=TCP`オプションを追加

これで「Run database migrations」ステップでもMySQLへの接続が正常に確立されるようになります。
